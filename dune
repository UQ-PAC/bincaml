; only build public package files with `dune build`.
; this excludes, e.g., tree-sitter test files

(alias
 (name default)
 (deps
  (package bincaml)
  bincaml.install)
 (package bincaml))

(alias
 (name default)
 (deps
  (package bincaml_lsp)
  bincaml_lsp.install)
 (package bincaml_lsp))
